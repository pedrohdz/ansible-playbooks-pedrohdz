---
- hosts: all
  name: User level configuration (does not run as sudo)
  tasks:
    - name: Place holder...
      ansible.builtin.debug:
        msg: place holder...

- hosts: all
  roles:
    # - role: macos-desktop
    #   when: ansible_os_family == 'Darwin'
    - role: git_user_config
    # - role: lafarer.osx-defaults
    #   when: ansible_os_family == 'Darwin'
  vars_files:
    - ['vars/home-{{ ansible_os_family }}.yaml', vars/home-defaults.yaml]
#     - vars/home-all.yaml
...
